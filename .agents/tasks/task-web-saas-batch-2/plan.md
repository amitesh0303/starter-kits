# Implementation Plan: Complete Tasks 9-13 (Web SaaS batch 2)

## Context and decisions (no design doc gaps found — following established repo patterns)

- Tasks 9 (`nextjs-booking-saas`), 10 (`nextjs-lms-saas`), and 11 (`nextjs-support-desk`) are **already fully implemented, tested, linted, type-checked, and built** on branch `feat/web-saas-batch-2` (commits `e6a476e`, `81f7df1`, `4384cd2`). Verified by re-running `pnpm test`/`lint`/`typecheck`/`build` in each — all green. FEAT-001, FEAT-002, FEAT-003 task files are marked `completed`. No rework needed; only cleanup items below apply to them.
- Task 12 (`nextjs-automation-saas`) is **partially scaffolded**: `.gitignore`, `package.json` (deps installed and verified), `starter.json`, `drizzle/schema.ts`, `drizzle/migrations/0000_initial.sql`, `drizzle.config.ts`, `domain/entities.ts` exist. Everything else (policies, lib/server/*, app/*, config files, tests, README, .env.example) is missing. FEAT-004 status is `in_progress`.
- Task 13 (`nextjs-file-saas`) has **not been started** — directory doesn't exist. FEAT-005 status is `pending`.
- Decision: build both remaining starters by directly mirroring the file-for-file pattern already proven in `nextjs-booking-saas` (Prisma+Stripe+webhook pattern), `nextjs-lms-saas` (Clerk+Stripe+fakes pattern), and `nextjs-b2b-saas`/`nextjs-support-desk` (Drizzle repository + policies pattern). This is faithful to the family blueprint in `design.md` and avoids re-deriving conventions the repo has already established (config boundary with `CONFIG_DECLARATIONS`/`validateConfig`/`isPlaceholderValue`, `providers.ts` fake-vs-real switching, `errors.ts` sanitized `AppError` subclasses, deny-by-default `domain/policies.ts`, `WebhookVerificationError` + HMAC signature verification, Property 4/5 test shape).
- Decision: `nextjs-automation-saas` and `nextjs-file-saas` both need a new local interface not yet present in any prior starter: `JobPort` (Inngest) with **bounded retry and terminal failure** (Property 8). Design it once for automation-saas, then reuse the same shape (adapted to conversion jobs) for file-saas, per requirement 3.14/3.15/10.14 and the `RetryPolicy` common interface in `design.md`.
- Decision: node/pnpm are only on PATH via `/opt/toolchains/.nvm/versions/node/v20.20.2/bin` in this sandbox — every verification command below must export that PATH first (already the pattern used to validate tasks 9-11).
- Cleanup decision: catalog/README status for all 5 starters (9-13) intentionally stays `pending`/`⏳ pending` — confirmed this matches the precedent set by `nextjs-b2b-saas`, which is fully built but still shows `pending` in `catalog/starters.json` and `README.md` (status promotion happens only in task 54.6 per `tasks.md`). Do not edit `catalog/starters.json` or root `README.md` for these starters.
- Cleanup decision: an orphaned, empty `nextjs-ai-saas/` directory (leftover from a prior branch, only containing a stray `tsconfig.tsbuildinfo`, not tracked in git) and a modified `.next` cache file must be cleaned from the working tree before committing, since they are unrelated build artifacts, not part of tasks 9-13.

## Plan

- [ ] 1. Clean the working tree of stray untracked artifacts unrelated to tasks 9-13.
      Remove the orphaned `nextjs-ai-saas/` directory (leftover build artifact, not tracked on this branch) and revert the accidental modification to `nextjs-b2b-saas/.next/cache/eslint/.cache_1jipya5`.
      Files: delete `nextjs-ai-saas/`; `git checkout -- nextjs-b2b-saas/.next/cache/eslint/.cache_1jipya5`
      Verify: `cd /projects/sandbox/starter-kits && git status --porcelain` shows no changes under `nextjs-ai-saas/` or `nextjs-b2b-saas/.next/`.

- [ ] 2. Add `domain/policies.ts` for `nextjs-automation-saas` with deny-by-default authorization and plan gating.
      Implement `AuthContext { userId }`, `canCreateWorkflow(ctx, currentWorkflowCount, planLimit)`, `canEditWorkflow(ctx, workflow)` (owner only), `canTriggerRun(ctx, workflow, subscription, runsThisMonth)` (owner + subscription status in `active`/`trialing` + `runsThisMonth < subscription.maxRunsPerMonth`), `canViewRun(ctx, workflow)` (owner only). Mirror the null/empty-userId denial style used in `nextjs-booking-saas/domain/policies.ts` and `nextjs-support-desk/domain/policies.ts`.
      Files: `nextjs-automation-saas/domain/policies.ts`
      Verify: no runnable command yet (types only); confirmed later by step 12's unit tests.

- [ ] 3. Add `lib/server/errors.ts` and `lib/server/config.ts` for `nextjs-automation-saas`.
      `errors.ts`: `AppError`, `AuthenticationError`, `AuthorizationError`, `NotFoundError`, `BillingError`, `WebhookVerificationError`, `PlanLimitError`, `RetryExhaustedError`, `sanitizeProviderError` (copy shape from `nextjs-booking-saas/lib/server/errors.ts`, add the two new automation-specific errors). `config.ts`: `ConfigSpec`/`CONFIG_DECLARATIONS`/`validateConfig`/`isPlaceholderValue`/`getRawConfig`/`getServerOnlyKeys`/`getPublicKeys` for `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` (client-safe), `DATABASE_URL`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `INNGEST_EVENT_KEY`, `INNGEST_SIGNING_KEY`, `NEXT_PUBLIC_APP_URL` (client-safe) — copy the aggregate-validation pattern verbatim from `nextjs-lms-saas/lib/server/config.ts`.
      Files: `nextjs-automation-saas/lib/server/errors.ts`, `nextjs-automation-saas/lib/server/config.ts`
      Verify: none yet (no test file references them until step 12); syntax checked implicitly by later `pnpm typecheck` in step 13.

- [ ] 4. Add `lib/server/auth.ts` (Clerk adapter) for `nextjs-automation-saas`.
      Provide `buildAuthContext(userId: string | null): AuthContext | null`, mirroring `nextjs-lms-saas/lib/server/auth.ts` but without the role field (automation-saas has no creator/learner distinction — every authenticated user owns their own workflows).
      Files: `nextjs-automation-saas/lib/server/auth.ts`
      Verify: covered by step 13's typecheck/test run.

- [ ] 5. Add `lib/server/database.ts` for `nextjs-automation-saas` with repository interfaces and in-memory fakes.
      Define `WorkflowRepository`, `RunRepository`, `StepAttemptRepository`, `SubscriptionRepository`, `EventRepository` interfaces plus `InMemory*Repository` implementations (id/createdAt/updatedAt generation, `findById`, `findByUserId` for workflows, `findByWorkflowId` for runs, monthly run counting for plan gating), mirroring the interface+in-memory-fake shape in `nextjs-b2b-saas/lib/server/database.ts`.
      Files: `nextjs-automation-saas/lib/server/database.ts`
      Verify: covered by step 13's typecheck/test run.

- [ ] 6. Add `lib/server/billing.ts` and `lib/server/billing-fake.ts` (Stripe subscription adapter) for `nextjs-automation-saas`.
      `billing.ts`: `BillingPort` (`createCheckout`, `verifyWebhook`, `handleWebhookEvent`), `StripeBillingAdapter` with HMAC-SHA256 `Stripe-Signature` verification (`t=...,v1=...` format) throwing `WebhookVerificationError` on mismatch — copy verbatim logic from `nextjs-lms-saas/lib/server/billing.ts` (same subscription-status state machine: `active|past_due|cancelled|trialing`). `billing-fake.ts`: `FakeBillingAdapter` with `validWebhookSecret = "whsec_test_secret"` string-match verification and idempotent `customer.subscription.updated/deleted` handling, copying `nextjs-lms-saas/lib/server/billing-fake.ts`.
      Files: `nextjs-automation-saas/lib/server/billing.ts`, `nextjs-automation-saas/lib/server/billing-fake.ts`
      Verify: covered by step 13's typecheck/test run; behavior specifically exercised by the Property 5 test added in step 12.

- [ ] 7. Add `lib/server/jobs.ts` and `lib/server/jobs-fake.ts` (Inngest adapter) for `nextjs-automation-saas` implementing bounded retry with terminal failure.
      `jobs.ts`: define `JobPort { triggerWorkflow(workflowId: string, triggeredBy: string): Promise<{ runId: string }>; getRunStatus(runId: string): Promise<RunStatus> }` and `InngestAdapter` wrapping the `inngest` SDK's `step.run`/retry semantics, with a `maxAttempts: number` constructor parameter — each failed attempt increments an attempt counter and records a `StepAttempt`; once `attemptNumber >= maxAttempts` the run transitions to terminal `failed` and stops retrying (never stays `pending`/`running` indefinitely). `jobs-fake.ts`: `FakeJobAdapter` that takes a scripted sequence of per-run outcomes (`"succeed"` or `"fail"` for each attempt) and deterministically simulates `pending -> running -> completed` (on success) or `pending -> running -> running -> ... -> failed` (terminal, after `maxAttempts` failures), recording every `StepAttempt` in memory for assertions.
      Files: `nextjs-automation-saas/lib/server/jobs.ts`, `nextjs-automation-saas/lib/server/jobs-fake.ts`
      Verify: covered by step 13's typecheck/test run; behavior specifically exercised by the Property 8 test added in step 12.

- [ ] 8. Add `lib/server/providers.ts` for `nextjs-automation-saas` wiring real-vs-fake adapters.
      `getProviders()`/`resetProviders()` returning `{ billing, jobs }`, selecting `FakeBillingAdapter`/`FakeJobAdapter` when `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`INNGEST_EVENT_KEY`/`INNGEST_SIGNING_KEY` are placeholder values (via `isPlaceholderValue` from step 3), else `StripeBillingAdapter`/`InngestAdapter` — copy the caching pattern from `nextjs-lms-saas/lib/server/providers.ts`.
      Files: `nextjs-automation-saas/lib/server/providers.ts`
      Verify: covered by step 13's typecheck/test run.

- [ ] 9. Add `middleware.ts`, `app/page.tsx`, `app/dashboard/page.tsx`, `app/api/webhooks/stripe/route.ts`, and `app/api/inngest/route.ts` for `nextjs-automation-saas`.
      `middleware.ts`: Clerk `clerkMiddleware` protecting `/dashboard(.*)`, copied from `nextjs-lms-saas/middleware.ts`. `app/page.tsx`: public landing page describing workflow automation with a CTA link to `/dashboard`. `app/dashboard/page.tsx`: protected dashboard with sections for "Workflows" (create/list), "Runs" (status, retry count), and "Plan" (monthly run usage vs `maxRunsPerMonth`) — same static-content-with-`className` structure as `nextjs-lms-saas/app/dashboard/page.tsx`. `app/api/webhooks/stripe/route.ts`: reads raw body + `Stripe-Signature` header, calls `providers.billing.verifyWebhook` before any mutation, checks `EventRepository.exists` for dedupe, then `handleWebhookEvent`, returns 200/400 — copy structure from `nextjs-lms-saas/app/api/webhooks/stripe/route.ts`. `app/api/inngest/route.ts`: Inngest `serve()` handler exposing the workflow-run Inngest function(s) built on `lib/server/jobs.ts`.
      Files: `nextjs-automation-saas/middleware.ts`, `nextjs-automation-saas/app/page.tsx`, `nextjs-automation-saas/app/dashboard/page.tsx`, `nextjs-automation-saas/app/api/webhooks/stripe/route.ts`, `nextjs-automation-saas/app/api/inngest/route.ts`
      Verify: covered by step 13's `pnpm build`.

- [ ] 10. Add remaining project config files and `.env.example` for `nextjs-automation-saas`.
      Copy `next.config.ts`, `postcss.config.mjs`, `vitest.config.ts` verbatim from `nextjs-lms-saas` (same Next 15/Tailwind 4/Vitest setup). `eslint.config.mjs`: same as `nextjs-lms-saas/eslint.config.mjs` but ignore `drizzle/**` instead of `prisma/**`. `tsconfig.json`: identical to `nextjs-lms-saas/tsconfig.json`. `app/layout.tsx` and `app/globals.css`: minimal root layout + Tailwind import, adapted title/description to "Automation SaaS Starter". `.env.example`: grouped placeholders for `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `DATABASE_URL`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `INNGEST_EVENT_KEY`, `INNGEST_SIGNING_KEY`, `NEXT_PUBLIC_APP_URL`.
      Files: `nextjs-automation-saas/next.config.ts`, `nextjs-automation-saas/postcss.config.mjs`, `nextjs-automation-saas/vitest.config.ts`, `nextjs-automation-saas/eslint.config.mjs`, `nextjs-automation-saas/tsconfig.json`, `nextjs-automation-saas/app/layout.tsx`, `nextjs-automation-saas/app/globals.css`, `nextjs-automation-saas/.env.example`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-automation-saas && pnpm install` completes without missing-config errors.

- [ ] 11. Add tests for `nextjs-automation-saas`: unit, integration, and smoke.
      `tests/unit/domain/policies.test.ts` (plan gating: under/at/over limit, ownership true/false cases). `tests/unit/server/billing.test.ts` (valid/invalid webhook signature, subscription state transitions) — adapt from `nextjs-lms-saas/tests/unit/server/billing.test.ts`. `tests/unit/server/jobs.test.ts` (bounded retry succeeds within limit, terminal failure after exhausting `maxAttempts`, attempt count never exceeds `maxAttempts`). `tests/integration/workflow.test.ts` (create workflow -> trigger run -> run reaches terminal state). `tests/integration/auth.test.ts` (unauthenticated denied dashboard actions, authenticated owner allowed, non-owner denied) — adapt from `nextjs-lms-saas/tests/integration/auth.test.ts`. `tests/smoke/build.test.ts` (module-import smoke checks for domain/policies, lib/server/config, errors, billing-fake, jobs-fake, database) — adapt from `nextjs-booking-saas/tests/smoke/build.test.ts`.
      Files: `nextjs-automation-saas/tests/unit/domain/policies.test.ts`, `nextjs-automation-saas/tests/unit/server/billing.test.ts`, `nextjs-automation-saas/tests/unit/server/jobs.test.ts`, `nextjs-automation-saas/tests/integration/workflow.test.ts`, `nextjs-automation-saas/tests/integration/auth.test.ts`, `nextjs-automation-saas/tests/smoke/build.test.ts`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-automation-saas && pnpm test` — all new test files pass (0 failures).

- [ ] 12. Add property tests for `nextjs-automation-saas`: Property 4, Property 5, and Property 8.
      `tests/property/auth-consistency.test.ts` (Property 4 — fast-check over random `userId`s and workflow owners: unauthenticated/wrong-user always denied, owner always allowed) — adapt generators from `nextjs-booking-saas/tests/property/auth-consistency.test.ts`. `tests/property/webhook-authenticity.test.ts` (Property 5 — fast-check over random signatures/payloads: invalid signature always throws `WebhookVerificationError` with zero state change; valid signature + same event ID applied twice produces at most one subscription-state transition) — adapt from `nextjs-b2b-saas/tests/property/webhook-authenticity.test.ts`. `tests/property/retry-termination.test.ts` (Property 8 — fast-check over random `maxAttempts` in `[1,10]` and random outcome sequences: run attempts never exceed `maxAttempts`, run reaches `completed` exactly once if any attempt in budget succeeds, else reaches terminal `failed` and never remains `pending`/`running`). Each test file must include the tag comment `Feature: multi-stack-boilerplates, Property N: <title>` per `design.md`'s testing strategy.
      Files: `nextjs-automation-saas/tests/property/auth-consistency.test.ts`, `nextjs-automation-saas/tests/property/webhook-authenticity.test.ts`, `nextjs-automation-saas/tests/property/retry-termination.test.ts`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-automation-saas && pnpm test` — all property tests pass with the configured `numRuns` (>=100 per requirement).

- [ ] 13. Write `nextjs-automation-saas/README.md` and run full standalone validation.
      README covers: tech stack, workflow/run/step-attempt model, Inngest local dev setup (`inngest dev`), retry/terminal-failure behavior, Stripe webhook setup, plan gating, `.env.example` walkthrough, scripts table, deployment notes — mirror the section structure of `nextjs-booking-saas/README.md`.
      Files: `nextjs-automation-saas/README.md`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-automation-saas && pnpm install && pnpm lint && pnpm typecheck && pnpm test && pnpm build` — all five commands exit 0.

- [ ] 14. Commit the completed `nextjs-automation-saas` starter and mark FEAT-004 completed.
      Update `.agents/tasks/task-web-saas-batch-2/features/FEAT-004.json` status to `"completed"` with a `findings` string summarizing test counts and any dependency-resolution notes (matching the style of FEAT-001/002/003 findings).
      Files: `.agents/tasks/task-web-saas-batch-2/features/FEAT-004.json`, all `nextjs-automation-saas/**` files from steps 2-13
      Verify: `cd /projects/sandbox/starter-kits && git add nextjs-automation-saas .agents/tasks/task-web-saas-batch-2/features/FEAT-004.json && git commit -m "feat: complete nextjs-automation-saas starter with workflow runs, bounded retry, and Stripe billing"` succeeds; `git log --oneline -1` shows the new commit.

- [ ] 15. Scaffold `nextjs-file-saas`: directory, `.gitignore`, `package.json`, `starter.json`.
      `.gitignore` identical to `nextjs-automation-saas/.gitignore`. `package.json`: exact-pinned deps matching `nextjs-automation-saas/package.json` plus `@aws-sdk/client-s3` (pin to the same `3.787.0` used in `nextjs-support-desk/package.json`) and `@aws-sdk/s3-request-presigner@3.787.0`; same devDeps/scripts (`dev/build/start/lint/typecheck/test/db:generate/db:migrate/db:push`) and the same `onlyBuiltDependencies` entries as `nextjs-automation-saas/package.json`. `starter.json`: copy the catalog id-8 entry verbatim from `catalog/starters.json` lines 211-234 (`folder: nextjs-file-saas`, `choices: "R2 + Inngest"`, `technologies: [TypeScript, Clerk, Neon, Drizzle, Cloudflare R2, Inngest, Stripe]`, `integrations: [Cloudflare R2, Inngest]`).
      Files: `nextjs-file-saas/.gitignore`, `nextjs-file-saas/package.json`, `nextjs-file-saas/starter.json`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-file-saas && pnpm install` completes and generates `pnpm-lock.yaml`.

- [ ] 16. Add `drizzle/schema.ts`, `drizzle/migrations/0000_initial.sql`, and `drizzle.config.ts` for `nextjs-file-saas`.
      Schema tables per FEAT-005 spec: `fileAssets` (id, userId, fileName, fileKey, fileSize, mimeType, status enum `uploaded|processing|ready|failed`, uploadedAt, createdAt), `conversionJobs` (id, fileAssetId FK, userId, outputFormat, status enum `pending|processing|completed|failed`, attempts, maxAttempts, error, startedAt, completedAt, createdAt), `outputAssets` (id, conversionJobId FK, fileKey, fileName, fileSize, mimeType, createdAt), `subscriptions` (id, userId, stripeCustomerId, stripeSubscriptionId, stripePriceId, status enum `active|past_due|cancelled|trialing`, currentPeriodEnd, cancelAtPeriodEnd, storageQuotaBytes, createdAt, updatedAt), `processedEvents` (id, providerEventId unique, eventType, processedAt) — follow the exact pgEnum/pgTable structuring style of `nextjs-automation-saas/drizzle/schema.ts`. Generate the matching initial SQL migration and `drizzle.config.ts` copied from `nextjs-automation-saas/drizzle.config.ts` (schema path only differs by folder).
      Files: `nextjs-file-saas/drizzle/schema.ts`, `nextjs-file-saas/drizzle/migrations/0000_initial.sql`, `nextjs-file-saas/drizzle.config.ts`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-file-saas && pnpm exec tsc --noEmit drizzle/schema.ts drizzle.config.ts` reports no type errors (or defer to step 25's full `pnpm typecheck`).

- [ ] 17. Add `domain/entities.ts` and `domain/policies.ts` for `nextjs-file-saas`.
      `entities.ts`: `FileStatus`, `JobStatus`, `SubscriptionStatus` unions plus `FileAsset`, `ConversionJob`, `OutputAsset`, `Subscription`, `ProcessedEvent` interfaces mirroring the schema in step 16 (same style as `nextjs-automation-saas/domain/entities.ts`). `policies.ts`: `AuthContext { userId }`, `ALLOWED_MIME_TYPES` and `MAX_FILE_SIZE_BYTES` constants, `canUploadFile(ctx, currentUsageBytes, fileSize, subscription)` (authenticated + `currentUsageBytes + fileSize <= subscription.storageQuotaBytes`), `canStartConversion(ctx, fileAsset, subscription)` (file owner + subscription status active/trialing), `canDownloadOutput(ctx, conversionJob)` (job owner only), `canViewFiles(ctx, fileAsset)` (owner only) — deny-by-default null/empty-userId checks like `nextjs-support-desk/domain/policies.ts`.
      Files: `nextjs-file-saas/domain/entities.ts`, `nextjs-file-saas/domain/policies.ts`
      Verify: covered by step 25's full test/typecheck run.

- [ ] 18. Add `lib/server/errors.ts` and `lib/server/config.ts` for `nextjs-file-saas`.
      `errors.ts`: `AppError`, `AuthenticationError`, `AuthorizationError`, `NotFoundError`, `BillingError`, `WebhookVerificationError`, `FileSizeError`, `FileTypeError`, `QuotaExceededError`, `RetryExhaustedError`, `sanitizeProviderError` — combine the file-error shapes from `nextjs-support-desk/lib/server/errors.ts` with the billing/retry errors from step 3's automation-saas `errors.ts`. `config.ts`: `ConfigSpec`/declarations for `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `DATABASE_URL`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `INNGEST_EVENT_KEY`, `INNGEST_SIGNING_KEY`, `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL`, `NEXT_PUBLIC_APP_URL` — same aggregate-validation pattern as step 3.
      Files: `nextjs-file-saas/lib/server/errors.ts`, `nextjs-file-saas/lib/server/config.ts`
      Verify: covered by step 25's full test/typecheck run.

- [ ] 19. Add `lib/server/auth.ts` and `lib/server/database.ts` for `nextjs-file-saas`.
      `auth.ts`: `buildAuthContext(userId)` Clerk adapter, same as step 4. `database.ts`: `FileAssetRepository`, `ConversionJobRepository`, `OutputAssetRepository`, `SubscriptionRepository`, `EventRepository` interfaces + `InMemory*Repository` fakes, including a `sumFileSizeByUserId` helper on `FileAssetRepository` for quota checks — mirror `nextjs-b2b-saas/lib/server/database.ts` structure.
      Files: `nextjs-file-saas/lib/server/auth.ts`, `nextjs-file-saas/lib/server/database.ts`
      Verify: covered by step 25's full test/typecheck run.

- [ ] 20. Add `lib/server/billing.ts` / `billing-fake.ts` and `lib/server/storage.ts` / `storage-fake.ts` for `nextjs-file-saas`.
      Billing files: copy step 6's Stripe subscription adapter/fake verbatim (same HMAC verification, same state machine) since file-saas uses the identical Stripe subscription model. Storage files: `ObjectStorePort { uploadFile, deleteFile, getSignedUrl }` and `R2ObjectStoreAdapter` using `@aws-sdk/client-s3`/`s3-request-presigner`, with `validateFile(fileSize, mimeType, maxSize, allowedTypes)` called **before** any `PutObjectCommand` — copy verbatim from `nextjs-support-desk/lib/server/storage.ts` (`ALLOWED_MIME_TYPES`, `MAX_FILE_SIZE`, `FileSizeError`/`FileTypeError`). `storage-fake.ts`: `FakeObjectStoreAdapter` enforcing the same `validateFile` call before storing into an in-memory `Map`.
      Files: `nextjs-file-saas/lib/server/billing.ts`, `nextjs-file-saas/lib/server/billing-fake.ts`, `nextjs-file-saas/lib/server/storage.ts`, `nextjs-file-saas/lib/server/storage-fake.ts`
      Verify: covered by step 25's full test/typecheck run.

- [ ] 21. Add `lib/server/jobs.ts` / `jobs-fake.ts` and `lib/server/providers.ts` for `nextjs-file-saas`.
      Jobs files: adapt step 7's bounded-retry `JobPort`/`InngestAdapter`/`FakeJobAdapter` to conversion semantics — `JobPort { startConversion(fileAssetId, outputFormat): Promise<{ jobId: string }>; getJobStatus(jobId): Promise<JobStatus> }`, same terminal-failure-after-`maxAttempts` guarantee, fake simulates `pending -> processing -> completed` (with an `OutputAsset` created) or `pending -> processing -> ... -> failed` after exhausting attempts. Providers file: `getProviders()` returning `{ billing, storage, jobs }`, selecting fakes based on placeholder detection for Stripe/R2/Inngest env vars — same caching pattern as step 8.
      Files: `nextjs-file-saas/lib/server/jobs.ts`, `nextjs-file-saas/lib/server/jobs-fake.ts`, `nextjs-file-saas/lib/server/providers.ts`
      Verify: covered by step 25's full test/typecheck run.

- [ ] 22. Add `middleware.ts`, `app/page.tsx`, `app/dashboard/page.tsx`, `app/api/webhooks/stripe/route.ts`, `app/api/inngest/route.ts`, and `app/api/upload/route.ts` for `nextjs-file-saas`.
      `middleware.ts`: Clerk middleware protecting `/dashboard(.*)` (same as step 9). `app/page.tsx`: public landing page describing the file conversion service. `app/dashboard/page.tsx`: protected dashboard with "Upload", "Conversions" (status list), and "Storage Quota" sections. `app/api/webhooks/stripe/route.ts`: same verified-webhook-before-mutation pattern as step 9. `app/api/inngest/route.ts`: Inngest `serve()` exposing the conversion-job function built on `lib/server/jobs.ts`. `app/api/upload/route.ts`: reads multipart/form body, calls `domain/policies.canUploadFile`, then `storage.uploadFile` (which validates MIME/size **before** any storage call per step 20), then persists a `FileAsset` row — returns 413/415 on validation failure without touching storage.
      Files: `nextjs-file-saas/middleware.ts`, `nextjs-file-saas/app/page.tsx`, `nextjs-file-saas/app/dashboard/page.tsx`, `nextjs-file-saas/app/api/webhooks/stripe/route.ts`, `nextjs-file-saas/app/api/inngest/route.ts`, `nextjs-file-saas/app/api/upload/route.ts`
      Verify: covered by step 25's `pnpm build`.

- [ ] 23. Add remaining project config files and `.env.example` for `nextjs-file-saas`.
      Copy `next.config.ts`, `postcss.config.mjs`, `vitest.config.ts`, `tsconfig.json`, `app/layout.tsx`, `app/globals.css` from `nextjs-automation-saas` (adjust title/description to "File Conversion SaaS Starter"). `eslint.config.mjs`: same as step 10, ignoring `drizzle/**`. `.env.example`: grouped placeholders for `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `DATABASE_URL`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `INNGEST_EVENT_KEY`, `INNGEST_SIGNING_KEY`, `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL`, `NEXT_PUBLIC_APP_URL`.
      Files: `nextjs-file-saas/next.config.ts`, `nextjs-file-saas/postcss.config.mjs`, `nextjs-file-saas/vitest.config.ts`, `nextjs-file-saas/eslint.config.mjs`, `nextjs-file-saas/tsconfig.json`, `nextjs-file-saas/app/layout.tsx`, `nextjs-file-saas/app/globals.css`, `nextjs-file-saas/.env.example`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-file-saas && pnpm install` completes cleanly.

- [ ] 24. Add unit and integration tests for `nextjs-file-saas`.
      `tests/unit/domain/policies.test.ts` (quota boundary cases, MIME/size allowlist, ownership true/false). `tests/unit/server/billing.test.ts` (adapted from step 11's automation-saas billing test). `tests/unit/server/storage.test.ts` (MIME/size validation enforced before any store call, adapted from `nextjs-support-desk/tests/unit/server/storage.test.ts`). `tests/unit/server/jobs.test.ts` (bounded retry, terminal failure, adapted from step 11's jobs test). `tests/integration/upload.test.ts` (upload -> conversion -> output-asset-created flow, including a rejected-upload case that never reaches storage). `tests/integration/auth.test.ts` (unauthenticated/wrong-owner denial, owner allowed). `tests/smoke/build.test.ts` (module-import smoke checks).
      Files: `nextjs-file-saas/tests/unit/domain/policies.test.ts`, `nextjs-file-saas/tests/unit/server/billing.test.ts`, `nextjs-file-saas/tests/unit/server/storage.test.ts`, `nextjs-file-saas/tests/unit/server/jobs.test.ts`, `nextjs-file-saas/tests/integration/upload.test.ts`, `nextjs-file-saas/tests/integration/auth.test.ts`, `nextjs-file-saas/tests/smoke/build.test.ts`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-file-saas && pnpm test` — all listed test files pass.

- [ ] 25. Add property tests (Property 4, 5, 8) for `nextjs-file-saas`, write README, and run full standalone validation.
      `tests/property/auth-consistency.test.ts` (Property 4 — ownership + quota: random userIds/quotas, unauthenticated/wrong-owner/over-quota always denied). `tests/property/webhook-authenticity.test.ts` (Property 5 — same shape as step 12, Stripe). `tests/property/retry-termination.test.ts` (Property 8 — conversion jobs terminate within `maxAttempts`, same shape as step 12). Each tagged `Feature: multi-stack-boilerplates, Property N: <title>`. README covers tech stack, file/job/output model, R2 setup, upload validation rules, Inngest conversion pipeline, retry/terminal-failure behavior, Stripe billing/quota, `.env.example` walkthrough, deployment — mirror `nextjs-support-desk/README.md` + `nextjs-automation-saas/README.md` section structure.
      Files: `nextjs-file-saas/tests/property/auth-consistency.test.ts`, `nextjs-file-saas/tests/property/webhook-authenticity.test.ts`, `nextjs-file-saas/tests/property/retry-termination.test.ts`, `nextjs-file-saas/README.md`
      Verify: `export PATH="/opt/toolchains/.nvm/versions/node/v20.20.2/bin:$PATH" && cd /projects/sandbox/starter-kits/nextjs-file-saas && pnpm install && pnpm lint && pnpm typecheck && pnpm test && pnpm build` — all five commands exit 0.

- [ ] 26. Commit the completed `nextjs-file-saas` starter and mark FEAT-005 completed.
      Update `.agents/tasks/task-web-saas-batch-2/features/FEAT-005.json` status to `"completed"` with a `findings` string summarizing test counts and dependency notes (same style as prior FEAT files).
      Files: `.agents/tasks/task-web-saas-batch-2/features/FEAT-005.json`, all `nextjs-file-saas/**` files from steps 15-25
      Verify: `cd /projects/sandbox/starter-kits && git add nextjs-file-saas .agents/tasks/task-web-saas-batch-2/features/FEAT-005.json && git commit -m "feat: add nextjs-file-saas starter with R2 storage, Inngest conversion jobs, and Stripe billing"` succeeds; `git log --oneline -1` shows the new commit.

- [ ] 27. Run the full standalone-copy validation (per requirements 2.3-2.7, 13.10, 13.11) for both new/completed starters, mark `task.json` completed, and push the branch.
      Copy `nextjs-automation-saas/` and `nextjs-file-saas/` each to an isolated temp path outside the repo, run `pnpm install && pnpm lint && pnpm typecheck && pnpm test && pnpm build` in each copy to prove no root/other-starter dependency, then delete the temp copies. Update `.agents/tasks/task-web-saas-batch-2/task.json` `status` to `"completed"`. Push branch `feat/web-saas-batch-2` via `github_push_to_remote` and open one PR covering both new commits (title referencing tasks 12-13; note in the PR body that tasks 9-11 were already completed and verified in this batch).
      Files: `.agents/tasks/task-web-saas-batch-2/task.json`
      Verify: both standalone copies pass all 5 commands with exit code 0; `git log --oneline feat/web-saas-batch-2 -3` shows the two new feat commits plus the task.json update; PR is created/branch is pushed and reachable at its GitHub URL.
