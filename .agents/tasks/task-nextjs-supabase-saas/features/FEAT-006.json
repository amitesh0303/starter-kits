{
  "id": "FEAT-006",
  "type": "fix",
  "description": "Address 4 review feedback items: placeholder detection substring check, invite flow fix (remove membership creation), webhook route dedup (use getProviders), non-atomic tenant+membership compensating action.",
  "status": "in_progress",
  "steps": [
    "Fix isPlaceholderValue to use substring check for 'placeholder' (case-insensitive)",
    "Fix invite page: remove membershipRepo.create, add comment explaining production invite-acceptance flow needed",
    "Fix webhook route: use getProviders().billing instead of local createBillingAdapter()",
    "Fix tenant creation: wrap in try-catch with compensating tenant delete on membership failure",
    "Verify all existing tests still pass"
  ],
  "acceptance_criteria": [
    "isPlaceholderValue('sk_test_placeholder') returns true",
    "isPlaceholderValue('whsec_placeholder') returns true",
    "isPlaceholderValue('re_placeholder') returns true",
    "isPlaceholderValue('price_placeholder') returns true",
    "Invite action sends email without creating a membership record",
    "Webhook route uses getProviders().billing, no local createBillingAdapter()",
    "Tenant creation has compensating delete if membership creation fails",
    "pnpm run lint, typecheck, test, and build all pass"
  ],
  "verification": [
    "cd nextjs-supabase-saas && pnpm run lint",
    "cd nextjs-supabase-saas && pnpm run typecheck",
    "cd nextjs-supabase-saas && pnpm run test",
    "cd nextjs-supabase-saas && pnpm run build"
  ],
  "blocked_reason": null,
  "findings": null
}
