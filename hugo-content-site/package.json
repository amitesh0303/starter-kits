{
  "name": "hugo-content-site",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --minify",
    "search:index": "pagefind --site public",
    "test": "vitest run"
  },
  "dependencies": {
    "pagefind": "1.3.0"
  },
  "devDependencies": {
    "vitest": "3.2.1"
  }
}
